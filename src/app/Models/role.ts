export class role {
    roleId: number;
    roleName: string;
  
    constructor(roleId: number, roleName: string, title: string) {
      this.roleId = roleId;
      this.roleName = roleName;
    }
  }